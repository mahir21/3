//Truthy values are values that contains non-empty string, number other than 0, Arrays and Objects and Functions. This are Truthy value.
if (true) {

    //This code will execute the block if it is true
}

if ("Hello") {
       
    //This code will execute the block if it True
}

if (42) {

    //This code will be executed as the number is not 42
}

if (["apple","banana"]) {

    //It is not a empty array
}

if ({key:"value"}) {
     
    //This will execute the block if it is not key value pair
}

if( function() )
{
    //This will execute the function() value.
}